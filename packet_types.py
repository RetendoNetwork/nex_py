# Constants
SYN_PACKET = 0x0
CONNECT_PACKET = 0x1
DATA_PACKET = 0x2
DISCONNECT_PACKET = 0x3
PING_PACKET = 0x4

# Valid types map
valid_types = {
    SYN_PACKET: True,
    CONNECT_PACKET: True,
    DATA_PACKET: True,
    DISCONNECT_PACKET: True,
    PING_PACKET: True,
}